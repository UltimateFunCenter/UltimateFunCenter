@page
@using Microsoft.AspNetCore.Authorization
@model UltimateFunCenter.Pages.Floors.IndexModel
@attribute [Authorize(Roles = "Admin")]

@{
    ViewData["Title"] = "Floors";
}

<h1>Floors</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Floor[0].Level)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Floor[0].Building)
            </th>
            <th>Actions</th> <!-- Action column for icon buttons -->
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Floor)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Level)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Building.Name)
                </td>
                <td>
                    <!-- Edit Button with Icon -->
                    <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-outline-primary btn-sm">
                        <i class="fas fa-edit"></i>
                    </a>

                    <!-- Details Button with Icon -->
                    <a asp-page="./Details" asp-route-id="@item.Id" class="btn btn-outline-info btn-sm">
                        <i class="fas fa-eye"></i>
                    </a>

                    <!-- Delete Button with Icon -->
                    <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
